# ===========================================
# CollabTask Production Environment Template
# ===========================================

# Strong password for SQL Server 'sa' user.
# Must meet SQL Server complexity requirements.
SA_PASSWORD=ChangeThis_StrongPassword123!

# Symmetric signing key for JWT authentication.
# Must be at least 32 characters long.
JWT_KEY=ReplaceWithA32BytesOrLongerSecretKey

# Docker image tag (optional, used in compose)
TAG=prod
