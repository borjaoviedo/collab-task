# ============================================
# CollabTask Development Environment Template
# ============================================

# Local SQL Server SA password.
# Used by both the SQL service and the API connection string.
SA_PASSWORD=YourLocalStrongPassword123!

# JWT secret key used for development authentication.
# Can be any random string (minimum 32 characters).
JWT_KEY=dev_32bytes_minimum_base64_or_random_string_XXXXXXXX

# Docker image tag (optional, used for local builds)
TAG=dev