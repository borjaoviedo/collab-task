# -----------------------------------------------------------------------------
# CollabTask - Example environment file
# Copy this to `.env` and adjust values for your local/dev/prod setup.
# -----------------------------------------------------------------------------

# SQL Server SA password (min. 8 chars, must include uppercase, lowercase, digits, symbols)
SA_PASSWORD=ChangeMe!12345

# JWT secret key (must be at least 32 bytes for HMAC-SHA256).
# Use a secure random string or base64.
JWT_KEY=changeme_please_replace_with_secure_32bytes_minimum_value

# JWT metadata
JWT_ISSUER=collabtask
JWT_AUDIENCE=collabtask

# Docker image tag for API (default: local)
TAG=local

